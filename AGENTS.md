# For AI agents

This repository is an AutoCAD skill (cad-headless). If the user wants it installed, follow **[INSTALL.md](INSTALL.md)**.

The skill itself — what to read when editing drawings — is `plugins/cad-headless/skills/cad-headless/SKILL.md`.
